#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsx-typescript/bin/jsx-tsc" "$@"
  ret=$?
else 
  node  "$basedir/../jsx-typescript/bin/jsx-tsc" "$@"
  ret=$?
fi
exit $ret
